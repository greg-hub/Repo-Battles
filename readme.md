Github battle - A Github comparison app. Who is most active wins!

npm run start    // builds app for development on http://localhost:8080/

npm run production  // builds the app for production, output in dist folder
